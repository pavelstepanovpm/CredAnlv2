import React from 'react';
import { Box, Typography } from '@mui/material';

export const DemoComponent: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">
        Демо компонент для показа Git workflow
      </Typography>
    </Box>
  );
};
